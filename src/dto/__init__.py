# Um DTO é o que o nome representa, um objeto que serve apenas para transferir dados.
# Na prática é como uma struct, que mantém dados de forma stateless afim de separar a
# lógica entre camadas (model), deslocando apenas valores.